import { globalFeaturesData } from './global.feature';
import { userFeatureData } from './user.feature';

const userFeatureState = userFeatureData;
const globalFeatures = globalFeaturesData;

export { userFeatureState, globalFeatures };
